# ==============================================================
#  PulseAI — Python Dependencies
#  Python 3.14  |  Last verified: June 2026
#
#  Install:  pip install -r requirements.txt
#  Upgrade:  pip install -r requirements.txt --upgrade
# ==============================================================


# --------------------------------------------------------------
#  Core ML
# --------------------------------------------------------------

# PyTorch — LSTM sequence model, training loop, inference
# Python 3.14 fully supported from 2.10+ (torch.compile included)
torch==2.12.1
dotenv

# NumPy — tensor operations, array utilities
# Note: NumPy 2.5 requires Python >= 3.12
numpy==2.5.0

# scikit-learn — AUC-ROC, F1, confusion matrix, train/test split
scikit-learn==1.9.0


# --------------------------------------------------------------
#  Data Processing
# --------------------------------------------------------------

# pandas — feature engineering, DataFrames, CSV/JSON handling
# pandas 3.0 requires Python >= 3.11  |  breaking changes from 2.x
# Key change: strings now default to str dtype, not object
pandas==3.0.3

# PyArrow — recommended alongside pandas 3.0 for string dtype performance
pyarrow>=20.0.0


# --------------------------------------------------------------
#  Databases
# --------------------------------------------------------------

# psycopg2-binary — PostgreSQL driver for Supabase connection
# Pure-binary build: no C compiler required on Windows
psycopg2-binary==2.9.12

# pymongo — MongoDB Atlas driver (official)
pymongo==4.17.0

# python-dotenv — loads .env credentials into os.environ
python-dotenv==1.1.0


# --------------------------------------------------------------
#  API
# --------------------------------------------------------------

# fastapi[standard] — REST API for model serving
# [standard] includes uvicorn, fastapi-cli, and email-validator
fastapi[standard]==0.136.3

# uvicorn[standard] — ASGI server (included via fastapi[standard])
# Listed explicitly for version pinning clarity
uvicorn[standard]==0.49.0


# --------------------------------------------------------------
#  Gradio Interface
# --------------------------------------------------------------

# gradio — 4-tab ML interface (Score, Top Risk, Timeline, Retrain)
# Deployed to Hugging Face Spaces
gradio==6.19.0


# --------------------------------------------------------------
#  Notebooks
# --------------------------------------------------------------

# jupyter — interactive development notebooks
jupyter>=1.1.0

# matplotlib — ROC curve plots, training loss curves
matplotlib>=3.10.0

# seaborn — confusion matrix heatmaps, feature distribution plots
seaborn>=0.13.2


# --------------------------------------------------------------
#  Testing
# --------------------------------------------------------------

# pytest — unit test runner
pytest>=8.4.0

# pytest-cov — code coverage reporting (target: > 70%)
pytest-cov>=6.0.0